# GitHub Desktop

Open Terminal and run:

```bash
cd ~/Downloads && sudo apt install -y ./GitHubDesktop-linux-amd64-3.4.13-linux1.deb && echo "GitHub Desktop installed successfully." && /usr/bin/github-desktop
```
