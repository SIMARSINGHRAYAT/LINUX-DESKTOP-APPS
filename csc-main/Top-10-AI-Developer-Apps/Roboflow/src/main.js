'use strict';
const { app, BrowserWindow, Menu, session, shell } = require('electron'); const fs=require('node:fs'); const path=require('node:path'); const APP_URL='https://app.roboflow.com/'; const PARTITION='persist:roboflow'; const APP_HOSTS=new Set(['app.roboflow.com','roboflow.com','www.roboflow.com','inference.roboflow.com']); const AUTH_HOSTS=new Set(['accounts.google.com','consent.google.com','oauth2.googleapis.com','gstatic.com','googleusercontent.com','appleid.apple.com','login.microsoftonline.com','auth0.com','okta.com']); let mainWindow; let statePath;
function allowed(h,s){return [...s].some(x=>h===x||h.endsWith(`.${x}`));} function trusted(v){try{const u=new URL(v);return u.protocol==='https:'&&(allowed(u.hostname.toLowerCase(),APP_HOSTS)||allowed(u.hostname.toLowerCase(),AUTH_HOSTS));}catch{return false;}} function external(v){if(v.startsWith('mailto:')||v.startsWith('tel:'))return void shell.openExternal(v);try{const u=new URL(v);if(u.protocol==='http:'||u.protocol==='https:')void shell.openExternal(u.toString());}catch{}}
function read(){try{const s=JSON.parse(fs.readFileSync(statePath,'utf8'));if(Number.isInteger(s.width)&&Number.isInteger(s.height))return s;}catch{}return{width:1400,height:900};} function save(){if(!mainWindow||mainWindow.isDestroyed())return;fs.mkdirSync(path.dirname(statePath),{recursive:true});fs.writeFileSync(statePath,JSON.stringify(mainWindow.getNormalBounds()));}
function policies(w){const c=w.webContents;c.setWindowOpenHandler(({url})=>trusted(url)?{action:'allow',overrideBrowserWindowOptions:{width:1100,height:800,title:'Roboflow Desktop',webPreferences:{preload:path.join(__dirname,'preload.js'),contextIsolation:true,nodeIntegration:false,sandbox:true,webSecurity:true,partition:PARTITION}}}:(external(url),{action:'deny'}));c.on('did-create-window',policies);for(const event of ['will-navigate','will-redirect'])c.on(event,(e,url)=>{if(!trusted(url)){e.preventDefault();external(url);}});c.on('did-redirect-navigation',()=>mainWindow?.focus());c.on('did-navigate',()=>mainWindow?.focus());c.on('will-download',(_e,i)=>i.setSavePath(path.join(app.getPath('downloads'),i.getFilename())));}
function menu(){Menu.setApplicationMenu(Menu.buildFromTemplate([{label:'File',submenu:[{role:'close'},{role:'quit'}]},{role:'editMenu'},{label:'View',submenu:[{role:'reload'},{role:'forceReload'},{role:'resetZoom'},{role:'zoomIn'},{role:'zoomOut'},{role:'togglefullscreen'},{role:'toggleDevTools',accelerator:'CommandOrControl+Shift+I'}]},{role:'windowMenu'},{role:'help',submenu:[{label:'Roboflow',click:()=>mainWindow?.loadURL(APP_URL)}]}]));} async function create(){statePath=path.join(app.getPath('userData'),'window-state.json');mainWindow=new BrowserWindow({...read(),minWidth:640,minHeight:480,title:'Roboflow Desktop',icon:path.join(__dirname,'..','resources','roboflow-desktop.png'),webPreferences:{preload:path.join(__dirname,'preload.js'),contextIsolation:true,nodeIntegration:false,sandbox:true,webSecurity:true,partition:PARTITION}});policies(mainWindow);mainWindow.on('resize',save);mainWindow.on('move',save);mainWindow.on('closed',()=>{mainWindow=null;});await mainWindow.loadURL(APP_URL);} if(!app.requestSingleInstanceLock())app.quit();else{app.on('second-instance',()=>{mainWindow?.restore();mainWindow?.focus();});app.whenReady().then(async()=>{const s=session.fromPartition(PARTITION);s.setUserAgent(app.userAgentFallback.replace(/\sElectron\/[^\s]+/,''));s.setPermissionRequestHandler((_w,p,cb)=>cb(p==='notifications'));menu();await create();app.on('activate',()=>{if(!BrowserWindow.getAllWindows().length)void create();});});app.on('window-all-closed',()=>{if(process.platform!=='darwin')app.quit();});app.on('before-quit',save);}
